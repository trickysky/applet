<map id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="{{scale}}" show-location></map>
<button id="recordBtn" class="{{btn_stat}}" disabled="{{location_authority}}" type="{{btn_type}}" bindtap="record">{{stat_text}}</button>
